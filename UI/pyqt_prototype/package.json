{
  "private": true,
  "type": "module",
  "scripts": {
    "generate-preview": "python3 generate_preview.py",
    "capture-preview": "node capture_preview.mjs",
    "capture-preview:fallback": "node capture_preview_fallback.mjs",
    "preview": "python3 generate_preview.py && (node capture_preview.mjs || node capture_preview_fallback.mjs)"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "sharp": "^0.35.4"
  }
}
